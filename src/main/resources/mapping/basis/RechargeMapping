<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="operations.basis.RechargeOperations">

    <resultMap id="RechargeMap" type="basis.Recharge">
        <!--TODO:在这里修改数据库定义-->
        <result property="rechargeId" column="id"/>
        <result property="userId" column="status"/>
        <result property="price" column="permission"/>
        <result property="date" column="userName"/>
    </resultMap>

    <select id="getRechargeById" resultMap="RechargeMap">
        SELECT * FROM Recharge
        <where>
            rechargeId = '${rechargeId}'
        </where>
    </select>
</mapper>